# SelfBlendedImages

- Upstream repo: https://github.com/mapooon/SelfBlendedImages
- Put the upstream source code in `repo/`
- Keep pretrained checkpoints in `weights/`

Current asset:
- `weights/FFc23_extracted/`

Note:
- This looks like an extracted PyTorch checkpoint. The upstream project normally expects a single checkpoint file in `weights/`, so you may want to re-download the original FF-c23 checkpoint file later.
